[tool.ruff]
target-version = "py311"
line-length = 100
exclude = [
  ".git",
  ".venv",
  "venv",
  "__pycache__",
  "logs",
  "storage",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]
